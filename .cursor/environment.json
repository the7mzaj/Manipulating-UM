{
  "name": "Manipulating-UM",
  "install": "sudo apt-get update && sudo apt-get install -y --no-install-recommends python3-tk python3-venv && python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/pip install pandas openpyxl"
}
